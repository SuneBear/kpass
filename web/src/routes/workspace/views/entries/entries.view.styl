@require '../../../../styles/ref'

.entriesView
  overflow: hidden

  > .cardBody
    background-color: $gray97

  .placeholderView,
  .card
    background: $white
    box-shadow: 0 1px 2px rgba(0, 0, 0, 0.1)

  .placeholderView
    // border: 1px dashed $brand
